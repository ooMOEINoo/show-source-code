import { signUpResponses, signInResponses } from './auth.responses';
import {
  getUsersResponses,
  getUserByIdResponses,
  updateUserResponses,
} from './user.responses';

export default {
  // auth responses
  signUpResponses,
  signInResponses,

  // user responses
  getUsersResponses,
  getUserByIdResponses,
  updateUserResponses,
};
