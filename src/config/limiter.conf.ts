export default {
  defaultLimitWindow: 60,
  // rate limiter option for auth routes
  authLimitAllowedHtis: 10,

  // rate limiter option for user routes
  userAllowedHtis: 10,

  // rate limiter option for category routes
  categoryAllowedHits: 10,
};
